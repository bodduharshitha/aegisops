apiVersion: v1
kind: Service
metadata:
  name: aegisops-api
  namespace: aegisops
spec:
  type: ClusterIP
  selector:
    app: aegisops-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000