apiVersion: v1
kind: Service
metadata:
  name: aegisops-api
  namespace: aegisops
  labels:
    app: aegisops-api
spec:
  type: ClusterIP
  selector:
    app: aegisops-api-test
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000