apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: aegisops-api
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - aegisops
  selector:
    matchLabels:
      app: aegisops-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
